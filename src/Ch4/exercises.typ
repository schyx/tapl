= An ML Implementation of Arithmetic Expressions

== 4.2.1

Putting `try` in a recursive call prevents the function from being tail-call recursive. A better way would be to
use `Maybe` types.

== 4.2.2

The definition of `eval` would be to evaluate the whole thing.
